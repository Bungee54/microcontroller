{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "assemble",
            "type": "shell",
            "command": "echo hi && echo 'cd src; ./assemble.sh PROGRAM.txt initial_ROM.txt' | bash",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}